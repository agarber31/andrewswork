{
  "name": "andrews-work",
  "version": "1.0.0",
  "private": true,
  "description": "Andrew's Work — project tracker + Tool Performance analytics, on one Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
